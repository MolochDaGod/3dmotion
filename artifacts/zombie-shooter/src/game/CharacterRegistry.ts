/**
 * CharacterRegistry.ts — compatibility shim.
 * Canonical definitions have moved to data/characters.ts.
 * This file re-exports everything so existing consumers compile unchanged.
 */
export * from "./data/characters";
